name: Teste de busca - 100 livros - timeout
on:
  push:
    paths:
      - 'tests/live-search-wrapper.mjs'
      - '.github/workflows/teste-busca-100-timeout.yml'
  workflow_dispatch:
jobs:
  teste:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Rodar 100 testes ao vivo
        run: node tests/live-search-wrapper.mjs
