const $=s=>document.querySelector(s);
const lista=v=>Array.isArray(v)?v:v==null?[]:[v];
const escapar=(v='')=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const normalizarTexto=v=>String(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();
const urlSegura=u=>{if(typeof u!=='string'||!u.trim())return'';try{const x=new URL(u,location.origin);return['http:','https:'].includes(x.protocol)?x.href:''}catch{return''}};
const estado={catalogo:null,consulta:'',resultados:[],fontes:{},buscando:false,token:0};
const traducoes=[['matematica basica','basic mathematics'],['aritmetica','arithmetic'],['fracao','fractions'],['porcentagem','percentages'],['razao e proporcao','ratios proportions'],['pre-algebra','prealgebra'],['pre algebra','prealgebra'],['algebra linear','linear algebra'],['algebra abstrata','abstract algebra'],['algebra','algebra'],['equacoes diferenciais','differential equations'],['equacao diferencial','differential equations'],['equacoes','equations'],['geometria analitica','analytic geometry'],['geometria diferencial','differential geometry'],['geometria','geometry'],['trigonometria','trigonometry'],['pre-calculo','precalculus'],['pre calculo','precalculus'],['calculo iii','multivariable calculus'],['calculo 3','multivariable calculus'],['calculo ii','integral calculus series'],['calculo 2','integral calculus series'],['calculo i','differential integral calculus'],['calculo 1','differential integral calculus'],['calculo','calculus'],['probabilidade','probability'],['estatistica','statistics'],['matematica discreta','discrete mathematics'],['logica matematica','mathematical logic'],['teoria dos conjuntos','set theory'],['demonstracoes','mathematical proofs'],['combinatoria','combinatorics'],['teoria dos grafos','graph theory'],['teoria dos numeros','number theory'],['analise real','real analysis'],['analise complexa','complex analysis'],['topologia','topology'],['metodos numericos','numerical analysis'],['otimizacao','optimization'],['pesquisa operacional','operations research'],['sistemas dinamicos','dynamical systems'],['matematica financeira','financial mathematics'],['teoria dos jogos','game theory'],['criptografia','cryptography'],['matematica para computacao','discrete mathematics computer science'],['matematica aplicada','applied mathematics']];
function termoApi(q){const n=normalizarTexto(q);for(const[a,b]of traducoes)if(n.includes(a))return b;return q}
async function carregarCatalogo(){if(estado.catalogo)return estado.catalogo;const r=await fetch('/dados/matematica.json?v=20260917m2',{headers:{Accept:'application/json'},cache:'no-cache'});if(!r.ok)throw Error('catalogo');estado.catalogo=await r.json();return estado.catalogo}
function fecharOutros(){for(const d of document.querySelectorAll('dialog[open]'))if(d.id!=='matematica')try{d.close()}catch{}}
function abrirMatematica(){const d=$('#matematica');if(!d)return;fecharOutros();if(!d.open){try{d.showModal()}catch{d.setAttribute('open','')}}carregarCatalogo().then(()=>{renderTopicos();renderizar()}).catch(()=>renderizar());setTimeout(()=>$('#campo-matematica')?.focus(),30)}
function renderTopicos(){const area=$('#topicos-matematica');if(!area||!estado.catalogo)return;area.innerHTML=lista(estado.catalogo.topicos).map(t=>`<button type="button" data-topico-mat="${escapar(t)}">${escapar(t)}</button>`).join('')}
function locais(q){const itens=lista(estado.catalogo?.materiais),n=normalizarTexto(q),generico=!n||['matematica','livro de matematica','apostila de matematica','curso de matematica'].includes(n);return itens.filter(x=>{if(generico)return true;const texto=normalizarTexto([x.titulo,x.descricao,x.nivel,...lista(x.topicos)].join(' ')),partes=n.split(/\s+/).filter(Boolean);return texto.includes(n)||partes.every(p=>texto.includes(p))}).map(x=>({...x,origem:'curado',provedor:x.fonte||'Acervo curado',url:urlSegura(x.url),peso:0}))}
function mapearOpenTextbook(d){const dados=Array.isArray(d)?d:lista(d?.data||d?.textbooks||d?.results),saida=[];for(const x of dados.slice(0,40)){const titulo=String(x?.title||x?.name||'').trim();if(!titulo)continue;const id=x?.id||titulo,url=urlSegura(x?.url||x?.web_url||x?.book_url||`https://open.umn.edu/opentextbooks/textbooks/${encodeURIComponent(id)}`);if(!url)continue;const descricao=String(x?.description||x?.abstract||x?.short_description||'Livro-texto aberto disponível na Open Textbook Library.').replace(/<[^>]+>/g,' ').replace(/\s+/g,' ').trim().slice(0,360);saida.push({id:`otl:${id}`,titulo,descricao,tipo:'Livro-texto completo · OER',nivel:'Superior',topicos:[],origem:'opentextbook',provedor:'Open Textbook Library',url,peso:10})}return saida}
function mapearOpenStax(d,q){const dados=[d?.items,d?.results,d?.books,d].find(Array.isArray)||[],termo=normalizarTexto(termoApi(q)),saida=[];for(const x of dados){const titulo=String(x?.title||x?.name||'').trim(),texto=normalizarTexto(`${titulo} ${x?.description||''} ${lista(x?.subjects).join(' ')}`);if(!titulo)continue;if(!/(math|algebra|calculus|statistics|precalculus|geometry|probability|trigonometry)/.test(texto))continue;if(termo&&!['matematica','mathematics'].includes(termo)&&!termo.split(/\s+/).some(p=>p.length>2&&texto.includes(p)))continue;const slug=x?.slug||x?.meta?.slug||'',url=urlSegura(x?.web_url||x?.url||(slug?`https://openstax.org/details/books/${slug}`:'https://openstax.org/subjects/math'));if(!url)continue;saida.push({id:`openstax:${x?.id||slug||titulo}`,titulo,descricao:String(x?.description||'Livro-texto completo, gratuito e revisado por educadores.').replace(/<[^>]+>/g,' ').replace(/\s+/g,' ').slice(0,340),tipo:'Livro completo · OpenStax',nivel:'Do médio ao superior',topicos:[],origem:'openstax',provedor:'OpenStax',url,peso:8})}return saida.slice(0,30)}
function mapearPressbooks(d){const saida=[];for(const h of lista(d?.hits?.hits)){const x=h?._source||{},titulo=String(x?.name||x?.headline||'').trim();if(!titulo)continue;const pagina=lista(x?.mainEntityOfPage)[0]||x?.mainEntityOfPage||{},url=urlSegura(typeof pagina==='string'?pagina:pagina?.id||pagina?.url||x?.id);if(!url)continue;const autores=lista(x?.creator).map(c=>typeof c==='string'?c:c?.name).filter(Boolean).slice(0,3).join(' · '),descricao=String(x?.description||'Livro educacional aberto publicado em ecossistema Pressbooks.').replace(/<[^>]+>/g,' ').replace(/\s+/g,' ').trim().slice(0,340);saida.push({id:`pressbooks:${h?._id||titulo}`,titulo,descricao:[autores,descricao].filter(Boolean).join(' — '),tipo:'Livro / curso aberto · Pressbooks',nivel:'Variável',topicos:[],origem:'pressbooks',provedor:'Pressbooks via OERSI',url,peso:20})}return saida.slice(0,30)}
function pdfOpenAlex(x){const locais=[x?.best_oa_location,x?.primary_location,...lista(x?.locations)].filter(Boolean);for(const l of locais){const u=urlSegura(l?.pdf_url);if(u&&l?.is_oa!==false)return u}const oa=urlSegura(x?.open_access?.oa_url);return oa&&/\.pdf(?:$|[?#])/i.test(oa)?oa:''}
function mapearOpenAlex(d){const saida=[];for(const x of lista(d?.results)){const pdf=pdfOpenAlex(x),titulo=String(x?.display_name||x?.title||'').trim();if(!pdf||!titulo)continue;const autores=lista(x?.authorships).map(a=>a?.author?.display_name).filter(Boolean).slice(0,4).join(' · ');saida.push({id:`openalex:${String(x?.id||titulo).split('/').pop()}`,titulo,descricao:[autores,x?.publication_year,x?.type].filter(Boolean).join(' · '),tipo:'PDF acadêmico aberto',nivel:'Complementar / avançado',topicos:[],origem:'openalex',provedor:'OpenAlex',url:pdf,peso:80})}return saida.slice(0,20)}
function ordenar(itens){const vistos=new Set(),saida=[];for(const x of itens){if(!x?.titulo||!x?.url)continue;const k=normalizarTexto(x.titulo).replace(/[^a-z0-9]+/g,' ').trim();if(vistos.has(k))continue;vistos.add(k);saida.push(x)}return saida.sort((a,b)=>(a.peso??99)-(b.peso??99)||(a.prioridade??999)-(b.prioridade??999))}
function simbolo(x){if(['curado','openstax','opentextbook'].includes(x.origem))return'∑';if(x.origem==='pressbooks')return'PB';return'PDF'}
function renderizar(){const area=$('#resultados-matematica');if(!area)return;const status=$('#status-matematica'),chips=$('#fontes-matematica-status'),carga=$('#carregando-matematica');if(status)status.textContent=estado.consulta?`${estado.resultados.length} ${estado.resultados.length===1?'material encontrado':'materiais encontrados'} · livros completos sempre priorizados`:'Do primeiro cálculo às áreas avançadas da matemática.';if(chips){const defs=[['curado','Acervo curado'],['openstax','OpenStax'],['opentextbook','Open Textbook Library'],['pressbooks','Pressbooks / OERSI'],['openalex','OpenAlex']];chips.hidden=!estado.consulta&&!estado.buscando;chips.innerHTML=defs.map(([id,n])=>`<span class="fonte-chip ${estado.fontes[id]?.erro?'erro':estado.fontes[id]?'ok':'espera'}"><b>${escapar(n)}</b><i>${estado.fontes[id]?.erro?'×':estado.fontes[id]?.quantidade??'…'}</i></span>`).join('')}
if(carga){carga.hidden=!estado.buscando;carga.setAttribute('aria-hidden',String(!estado.buscando));if(estado.buscando){$('#carregando-matematica-titulo').textContent='Abrindo livros-texto, apostilas e cursos completos…';$('#carregando-matematica-fonte').textContent='Acervo curado e OpenStax primeiro; depois Open Textbook, Pressbooks e PDFs acadêmicos.';$('#progresso-matematica-barra').style.width='68%';$('#progresso-matematica-texto').textContent='Consultando 5 camadas de acervo'}else if(estado.consulta){$('#progresso-matematica-barra').style.width='100%';$('#progresso-matematica-texto').textContent='Busca concluída'}}
if(!estado.consulta&&!estado.buscando){area.innerHTML='<div class="estado-vazio"><h3>Matemática do zero ao avançado.</h3><p>Escolha um assunto acima ou pesquise por aritmética, álgebra, geometria, cálculo, álgebra linear, equações diferenciais, probabilidade, estatística, análise real, topologia, teoria dos números e muito mais.</p></div>';return}if(estado.buscando&&!estado.resultados.length){area.innerHTML='<div class="estado-vazio"><h3>Procurando material completo…</h3><p>Os livros-texto e cursos completos recebem prioridade sobre artigos e PDFs isolados.</p></div>';return}area.innerHTML=estado.resultados.length?estado.resultados.map(x=>`<article class="resultado resultado-matematica"><div class="sem-capa capa-matematica" aria-hidden="true">${escapar(simbolo(x))}</div><div><h3>${escapar(x.titulo)}</h3><p>${escapar(x.descricao||'Material de matemática.')}</p><div class="meta-livro"><span>${escapar(x.tipo||'Material completo')}</span>${x.nivel?`<span>${escapar(x.nivel)}</span>`:''}<span>${escapar(x.provedor||'Fonte aberta')}</span></div>${lista(x.topicos).length?`<div class="tags-matematica">${lista(x.topicos).slice(0,6).map(t=>`<span>${escapar(t)}</span>`).join('')}</div>`:''}<div class="acoes-resultado"><a class="ler" href="${escapar(x.url)}" target="_blank" rel="noopener noreferrer">${/pdf/i.test(x.tipo||'')?'Abrir material / PDF':'Abrir livro'} ↗</a></div></div></article>`).join(''):'<div class="estado-vazio"><h3>Nenhum material apareceu nesta tentativa.</h3><p>Tente um assunto mais amplo, como “álgebra”, “cálculo”, “probabilidade” ou “análise”.</p></div>'}
}
async function buscarMatematica(q){q=String(q||'').trim();estado.consulta=q;estado.resultados=[];estado.fontes={};const token=++estado.token;if(!q){estado.buscando=false;renderizar();return}estado.buscando=true;renderizar();try{await carregarCatalogo();const base=locais(q);estado.resultados=ordenar(base);estado.fontes.curado={quantidade:base.length};renderizar();const c=new AbortController(),timer=setTimeout(()=>c.abort(),18000),json=async rota=>{try{const r=await fetch(rota,{signal:c.signal,headers:{Accept:'application/json'}});if(!r.ok)return null;return await r.json()}catch{return null}},t=encodeURIComponent(termoApi(q));const[os,otl,pb,oa]=await Promise.all([json(`/api/acervos-gratuitos?fonte=openstax&q=${t}`),json(`/api/acervos-gratuitos?fonte=opentextbook&q=${t}`),json(`/api/pressbooks?q=${t}`),json(`/api/acervos-gratuitos?fonte=openalex&q=${t}`)]);clearTimeout(timer);if(token!==estado.token)return;const grupos=[mapearOpenStax(os,q),mapearOpenTextbook(otl),mapearPressbooks(pb),mapearOpenAlex(oa)];estado.fontes.openstax={quantidade:grupos[0].length,erro:!os};estado.fontes.opentextbook={quantidade:grupos[1].length,erro:!otl};estado.fontes.pressbooks={quantidade:grupos[2].length,erro:!pb};estado.fontes.openalex={quantidade:grupos[3].length,erro:!oa};estado.resultados=ordenar([...base,...grupos.flat()])}catch{estado.fontes={curado:{quantidade:estado.resultados.length},openstax:{quantidade:0,erro:true},opentextbook:{quantidade:0,erro:true},pressbooks:{quantidade:0,erro:true},openalex:{quantidade:0,erro:true}}}if(token!==estado.token)return;estado.buscando=false;renderizar()}
function injetarMenu(){const conteudo=$('#conteudo-painel');if(!conteudo||conteudo.querySelector('[data-matematica="abrir"]'))return;const explorar=conteudo.querySelector('[data-acao="explorar"]');if(!explorar)return;const bloco=document.createElement('div');bloco.className='item-painel';bloco.innerHTML='<button type="button" data-matematica="abrir"><strong>Matemática</strong><small>Do básico à análise, álgebra, cálculo e áreas avançadas ↗</small></button>';explorar.closest('.item-painel')?.after(bloco)}
function iniciar(){const btn=$('#abrir-matematica');if(btn){btn.type='button';btn.dataset.matematica='abrir';btn.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();abrirMatematica()},true)}const form=$('#form-matematica');if(form)form.addEventListener('submit',e=>{e.preventDefault();buscarMatematica($('#campo-matematica')?.value||'')});injetarMenu();const alvo=$('#conteudo-painel');if(alvo)new MutationObserver(injetarMenu).observe(alvo,{childList:true,subtree:true});carregarCatalogo().then(renderTopicos).catch(()=>{})}
document.addEventListener('click',e=>{const top=e.target.closest?.('[data-topico-mat]');if(top){e.preventDefault();const q=top.getAttribute('data-topico-mat')||'';if($('#campo-matematica'))$('#campo-matematica').value=q;buscarMatematica(q);return}const b=e.target.closest?.('[data-matematica="abrir"]');if(!b)return;e.preventDefault();e.stopPropagation();abrirMatematica()},true);
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',iniciar,{once:true});else iniciar();
window.BibliotecaMatematica={abrir:abrirMatematica,pesquisar:buscarMatematica};